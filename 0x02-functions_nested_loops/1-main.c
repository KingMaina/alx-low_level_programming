#include <string.h>
#include "main.h"

/**
 * main - Enty point of application.
 *
 * Description: Prints the alphabet in lowercase followed by a newline.
 * Return: 0 (Success)
 */
int main(void)
{
	print_alphabet();
	return (0);
}
