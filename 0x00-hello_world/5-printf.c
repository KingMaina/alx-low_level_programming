#include <stdio.h>

/*
 * main - Entry point
 *
 * Prints a string to stdout using printf()
 *
 * Returns: 0 (sucess)
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
