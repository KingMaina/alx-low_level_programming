#include <stdio.h>
#include "main.h"

/**
* main - Entry point
*
* Return: Always 0
*/
int main(void)
{
	int length;

	length = _strlen("Puts with recursion");
	printf("Length of string is: %d\n", length);

	return (0);
}
